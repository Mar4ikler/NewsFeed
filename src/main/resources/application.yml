spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/skillbox
    username: mark
    password: 1111
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    enabled: false
    drop-first: true
    default-schema: public
logging:
  level:
    com.zaxxer.hikari: DEBUG
    liquibase: DEBUG